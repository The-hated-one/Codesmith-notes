

    // typeDefs.definitions.forEach((def) => {
    //   if (isObjectTypeDefinitionNode(def)) {
    //     if (def.name.value === 'Query') {
    //       (def.fields || []).forEach((field) => {
    //         if (field.name.value === 'pets') {
    //           // console.log(`field`, field)
    //           // field.type.type.name.value
    //           console.log(`field.type.type.type.name.value`, field.type.type)
    //           console.log(`field.type.type.type.name.value`, field.type.type.type.name.value)
    //         }
    //       })
    //     }
    //   }
    // })